<?php
namespace GMFramework;

/**
 * Заготовка класса дебаг утилит. Пока не включена во фреймворк
 */
class DebugUtils
{
/*
function logStackTrace()
{
    $arr = debug_backtrace();
    $lines = array();

    foreach ($arr as $call) 
    {
        $lines[] = sprintf('%s:%s %s(%s)', 
            $call['file'], $call['line'], $call['function'], implode(', ', $call['args']));
    }

    error_log(implode("\n", $lines));
}

 */
}
?>